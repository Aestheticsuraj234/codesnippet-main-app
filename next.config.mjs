/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['img.icons8.com' , "res.cloudinary.com" , "placehold.co" , "avatars.githubusercontent.com"],
            
    }
};

export default nextConfig;
